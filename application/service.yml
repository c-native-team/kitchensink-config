apiVersion: v1
kind: Service
metadata:
  annotations:
    description: The web server's http port.
  labels:
    application: kitchensink
  name: kitchensink
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    deployment: kitchensink
